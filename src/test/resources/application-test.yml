spring:
  config:
    activate:
      on-profile: test

  ai:
    openai:
      api-key: test-key
      chat:
        model: gpt-4o-mini
logging:
  level:
    root: WARN
    com.org: DEBUG

app:
  security:
    injection-guard:
      enabled: true
management:
  tracing:
    export:
      enabled: false
